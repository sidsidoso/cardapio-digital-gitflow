{
  "name": "cardapio-digital",
  "version": "1.0.1",
  "private": true,
  "description": "Projeto acadêmico para praticar GitFlow",
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "start": "node scripts/preview.cjs",
    "test": "node --test",
    "check": "node --check app.js && node --check test/cardapio.test.cjs && node --check scripts/preview.cjs"
  }
}
